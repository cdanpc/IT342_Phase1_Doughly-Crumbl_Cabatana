<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DoughlyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoughlyCrumbl"
        tools:targetApi="30">

        <!-- Launcher -->
        <activity
            android:name=".auth.ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DoughlyCrumbl">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".auth.ui.LoginActivity" android:exported="false" />
        <activity android:name=".auth.ui.RegisterActivity" android:exported="false" />

        <!-- Customer host + sub-screens -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".cart.ui.CheckoutActivity" android:exported="false" />
        <activity android:name=".orders.ui.OrderDetailActivity" android:exported="false" />

        <!-- Admin host + sub-screens -->
        <activity android:name=".admin.ui.AdminActivity" android:exported="false" />
        <activity android:name=".admin.ui.AdminAddEditProductActivity" android:exported="false" />
        <activity android:name=".admin.ui.AdminOrderDetailActivity" android:exported="false" />

        <!-- Profile info screens -->
        <activity android:name=".profile.ui.CareGuideActivity" android:exported="false" />
        <activity android:name=".profile.ui.AboutFaqActivity" android:exported="false" />
        <activity android:name=".profile.ui.PaymentInstructionsActivity" android:exported="false" />
        <activity android:name=".notifications.ui.NotificationDetailActivity" android:exported="false" />

    </application>

</manifest>
